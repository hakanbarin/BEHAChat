                                                                      Table "public.users"
      Column      |            Type             | Collation | Nullable |              Default              | Storage  | Compression | Stats target | Description 
------------------+-----------------------------+-----------+----------+-----------------------------------+----------+-------------+--------------+-------------
 id               | integer                     |           | not null | nextval('users_id_seq'::regclass) | plain    |             |              | 
 username         | character varying(50)       |           | not null |                                   | extended |             |              | 
 password_hash    | character varying(255)      |           | not null |                                   | extended |             |              | 
 permission_level | integer                     |           |          | 2                                 | plain    |             |              | 
 is_online        | boolean                     |           |          | false                             | plain    |             |              | 
 last_activity    | timestamp without time zone |           |          | CURRENT_TIMESTAMP                 | plain    |             |              | 
 created_at       | timestamp without time zone |           |          | CURRENT_TIMESTAMP                 | plain    |             |              | 
 updated_at       | timestamp without time zone |           |          | CURRENT_TIMESTAMP                 | plain    |             |              | 
Indexes:
    "users_pkey" PRIMARY KEY, btree (id)
    "idx_users_online" btree (is_online)
    "idx_users_permission" btree (permission_level)
    "idx_users_username" btree (username)
    "users_username_key" UNIQUE CONSTRAINT, btree (username)
Check constraints:
    "users_permission_level_check" CHECK (permission_level >= 0 AND permission_level <= 4)
Referenced by:
    TABLE "bans" CONSTRAINT "bans_banned_by_id_fkey" FOREIGN KEY (banned_by_id) REFERENCES users(id) ON DELETE SET NULL
    TABLE "bans" CONSTRAINT "bans_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
    TABLE "session_logs" CONSTRAINT "session_logs_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
    TABLE "tokens" CONSTRAINT "tokens_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
Triggers:
    update_users_updated_at BEFORE UPDATE ON users FOR EACH ROW EXECUTE FUNCTION update_updated_at_column()
Access method: heap

